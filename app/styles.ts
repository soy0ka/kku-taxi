import { StyleSheet } from 'react-native'

export const MiddleContainer = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
})

export default {
  MiddleContainer
}